{
  "type": "commonjs",
  "name": "simplest-discord-webhook",
  "version": "1.0.2",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "publish": "npm publish",
    "build": "esbuild ./src/index.js --format=esm --bundle --outfile=./dist/index.js --platform=node --target=node17.4 --minify",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "author": "LyricalString",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.27.2",
    "discord.js": "^13.6.0",
    "simplest-discord-webhook": "^1.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.14.38",
    "jest": "^28.0.2",
    "jest-cli": "^28.0.2"
  }
}
